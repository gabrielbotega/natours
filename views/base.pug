//- The base here is the skeleton of the HTML
doctype html
html(lang='en')
    head
        block head 
            meta(charset='UTF-8')
            meta(http-equiv='X-UA-Compatible', content='IE=edge')
            meta(name='viewport', content='width=device-width, initial-scale=1.0')
            link(rel='stylesheet' href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            


            title Natours | #{title}
<!--The content defined in the tour.pug (script) will appear here-->
    body
        // HEADER
        include _header

        // CONTENT
        
        block content
            h1 This is a placeholder heading

        // FOOTER
        include _footer

        //- It's not ideal to have several scripots here. It's better for performance to have a single bundled file in order to reduce the amount of requests. That's why we use Parcel
        //- script(src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.4.0/axios.min.js")   //- Since I do not include axios this way, need to install it
        //- script(src="/js/mapbox.js")
        //- script(src="/js/login.js")
        
        script(src="/js/bundle.js")

      
        