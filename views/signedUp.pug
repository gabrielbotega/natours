extends base

block content

    main.main
    .error
        .error__title
        h2.heading-secondary.heading-secondary--signedup= `Welcome ${user.name.split(" ")[0]}!`   
        h2.error__emoji 🎉🎆
        //-.error__msg= msg
